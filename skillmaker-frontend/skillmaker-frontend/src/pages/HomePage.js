import { Link, useNavigate } from "react-router-dom";import { Navbar, Nav } from "react-bootstrap";

const HomePage = () => {
    return(
        <div>
            <h1 className="text-center">Dashboard</h1>
        </div>
    );
};

export default HomePage;